composer update

// limpa a bd e cria tabelas
php artisan migrate:fresh

// inseres dados default - seeds 
php artisan db:seed    

// inicia serve -- login so funciona assim em localhost
php artisan serve   